# 融合 PCA 与因子分析的电商用户复购预测系统 需求规格说明书

## 文档基础信息

1. 项目全称：融合 PCA 与因子分析的电商用户复购预测系统（SVM、随机森林对比建模）
2. 开发 Python 版本：3.13.2
3. 架构模式：前后端分离架构
4. 后端框架：FastAPI
5. 前端：数据可视化大屏（动态浮动、高质感商务 UI，用于线上部署展示，求职 HR 演示）
6. 核心技术栈明细

### 后端 Python 技术栈清单

数据处理：pandas、numpy

可视化绘图：seaborn、matplotlib

机器学习 / 降维：scikit-learn（PCA、FactorAnalysis、SVM、RandomForest、数据集划分、评估指标）

数值计算：scipy

接口服务：fastapi、uvicorn

文件读写：openpyxl、pyarrow（parquet）

工具：pydantic、python-multipart、jsonable_encoder

### 前端技术栈

基础：Vue3 + Vite

UI 组件库：Element Plus

可视化图表：ECharts 5、DataV（大屏专用）

动画浮动效果：GSAP、CSS3 动态浮动动画

路由：Vue Router

网络请求：Axios

样式：SCSS、自适应大屏布局

## 1 引言

### 1.1 编写目的

本文档完整定义本电商用户复购预测系统全部功能、非功能、接口、文件结构、部署需求，作为开发、演示、毕业设计 / 项目作品集交付依据；系统面向求职 HR 线上演示，兼顾机器学习建模实验与可视化大屏展示双重能力。

### 1.2 项目目标

1. 基于 8500 条电商用户数据集完成数据清洗、缺失值处理、特征标准化
2. 实现 PCA、因子分析两种降维算法融合处理高维 36 维用户特征
3. 搭建 SVM、随机森林二分类模型，完成复购预测并对比模型精度
4. FastAPI 后端封装全部数据、降维、建模、指标绘图接口
5. 高端动态可视化大屏前端，实时拉取后端数据，动态渲染指标、图表、用户分层看板
6. 支持在线导出数据集、建模报告、可视化图片，线上部署可直接访问演示

### 1.3 文档受众

开发人员、毕业设计评审、求职 HR、项目演示评审人员

### 1.4 术语定义

- 复购标签 repurchase_label：1=30 天复购用户，0 = 流失用户
- PCA：主成分分析，线性降维
- 因子分析：提取潜在消费公因子，和 PCA 融合组合特征
- 前后端分离：前端仅负责展示，所有计算、建模、数据处理交由 FastAPI 后端
- 动态浮动大屏：非静态页面，图表自动轮播、数字滚动、模块悬浮浮动动画、实时数据刷新

## 2 项目整体文件目录结构

plaintext

```
ecommerce_repurchase_system/
├── dataset/                    # 数据集文件夹
│   └── ecommerce_repurchase_8500.csv  # 8500条用户原始数据集
├── data_generate/
│   └── generate_dataset.py    # 数据集生成脚本（第一部分提供）
├── backend/                    # FastAPI后端完整工程
│   ├── main.py                 # FastAPI程序入口，启动服务
│   ├── config.py               # 全局配置：文件路径、模型参数、端口、阈值
│   ├── schemas/                # Pydantic数据校验模型
│   │   ├── data_schema.py      # 数据集入参、返回格式定义
│   │   └── model_schema.py     # 建模请求、评估指标返回体
│   ├── core/                   # 核心算法模块
│   │   ├── data_clean.py       # 缺失值、异常值、标准化处理
│   │   ├── dim_reduce.py       # PCA+因子分析融合降维逻辑
│   │   ├── model_train.py       # SVM、随机森林训练、预测、对比
│   │   └── visual_draw.py      # seaborn/matplotlib绘图，生成图表Base64
│   ├── api/                    # 接口路由分层
│   │   ├── data_api.py         # 数据集读取、预览、导出接口
│   │   ├── dim_api.py          # 降维分析接口（PCA/因子分析结果）
│   │   ├── train_api.py        # 模型训练、预测、指标对比接口
│   │   └── screen_api.py       # 大屏专用实时数据接口
│   ├── utils/                  # 通用工具函数
│   │   ├── file_util.py        # 文件读写、csv/parquet导出
│   │   └── math_util.py        # 数据统计计算
│   ├── static/                 # 后端生成图表缓存目录
│   └── requirements.txt        # 后端全部Python依赖（适配3.13.2）
├── frontend/                   # Vue3可视化大屏前端工程
│   ├── public/
│   ├── src/
│   │   ├── api/                # axios请求后端接口封装
│   │   ├── components/         # 大屏浮动组件
│   │   │   ├── FloatCard.vue   # 浮动数据卡片（带上下悬浮动画）
│   │   │   ├── ChartBox.vue    # 动态图表容器
│   │   │   ├── NumScroll.vue   # 数字滚动动画组件
│   │   │   └── SidePanel.vue   # 侧边筛选面板
│   │   ├── views/
│   │   │   └── ScreenDash.vue  # 主可视化大屏页面
│   │   ├── router/index.js
│   │   ├── style/              # 全局大屏渐变、浮动动画样式
│   │   └── App.vue
│   ├── vite.config.js
│   └── package.json
├── output/                     # 系统输出文件
│   ├── model/                  # 训练保存的SVM、随机森林模型
│   ├── report/                 # 建模评估报告（csv）
│   └── image/                  # 分析图表
├── docs/
│   ├── 数据集详细说明书.md
│   ├── 需求规格说明书.md      # 本文档
│   └── 项目开发方案.md
└── README.md                   # 项目部署、启动教程（HR演示专用）
```

## 3 后端功能需求（FastAPI）

### 3.1 数据管理模块

1. 数据集加载接口：读取 dataset 下 csv，返回样本量、特征数量、缺失值统计、复购正负比例
2. 数据预览接口：分页返回前 N 行原始数据，支持前端表格渲染
3. 数据清洗接口：自动填充缺失值、剔除极端异常值、特征标准化，返回清洗后数据
4. 数据集导出接口：支持前端一键下载清洗后数据集 csv 文件

### 3.2 降维算法核心模块（项目核心）

1. 独立 PCA 降维接口：输出主成分方差贡献率、碎石图数据、降维后特征矩阵
2. 独立因子分析接口：输出公因子方差、因子载荷矩阵、因子得分
3. PCA + 因子分析融合接口：将 PCA 主成分与因子得分拼接融合新特征，供模型训练
4. 降维可视化接口：生成特征热力图、二维降维散点图，以 Base64 图片返回前端

### 3.3 机器学习建模对比模块

1. 数据集划分接口：自动划分训练集 / 测试集（7:3）
2. 随机森林训练接口：支持自定义树深度、树数量，输出训练耗时、特征重要性
3. SVM 训练接口：支持线性 / 高斯核，输出分类边界指标
4. 模型对比接口：统一返回两种模型完整评估指标：准确率、精确率、召回率、F1、AUC
5. 批量预测接口：传入单用户特征，输出复购概率与分类结果
6. 建模报告生成接口：导出模型对比指标报表至 output/report

### 3.4 大屏专用实时数据接口（前端专用）

专门为可视化大屏提供轻量化实时统计数据，每 30 秒自动刷新：

1. 全局总指标：总用户数、复购用户数、整体复购转化率、流失用户数量
2. 用户分层统计：会员等级、城市层级、设备类型复购分布
3. 消费指标统计：平均消费、优惠券使用率、静默用户占比
4. 模型实时指标：两种模型当前 AUC、准确率动态数值
5. 特征 Top10 重要性数据，供给前端柱状图渲染

### 3.5 绘图接口（matplotlib/seaborn）

后端统一生成专业分析图表，Base64 传输至前端大屏展示：

1. 特征相关性热力图
2. PCA 方差贡献率碎石图
3. 因子载荷矩阵分布图
4. 随机森林特征重要性柱状图
5. SVM & 随机森林 ROC 对比曲线
6. 用户消费金额分布箱线图

### 3.6 后端文件说明（每个文件作用）

表格

|          文件路径           |                     核心技术 & 功能说明                      |
| :-------------------------: | :----------------------------------------------------------: |
|       backend/main.py       |  FastAPI 服务启动入口，跨域配置，路由挂载，uvicorn 运行入口  |
|      backend/config.py      |    pandas 全局读取配置、模型超参、图片缓存路径、服务端口     |
| backend/core/data_clean.py  | numpy+pandas 实现缺失值填充、异常值裁剪、标准化 StandardScaler |
| backend/core/dim_reduce.py  | sklearn.decomposition.PCA/ FactorAnalysis，融合降维特征拼接逻辑 |
| backend/core/model_train.py | SVC、RandomForestClassifier 训练、网格搜索、评估指标计算、模型持久化 joblib |
| backend/core/visual_draw.py | matplotlib+seaborn 绘制全部分析图表，图片转 base64 返回前端  |
|  backend/api/screen_api.py  |      大屏轻量化统计接口，聚合各类用户指标，高频快速响应      |
| backend/utils/file_util.py  |   pandas 读写 csv/parquet，文件压缩导出，自动创建输出目录    |
|  backend/requirements.txt   | 记录所有依赖：numpy==2.1、pandas==2.2、scikit-learn==1.5、fastapi、uvicorn、seaborn、matplotlib、scipy、joblib 等，兼容 Python3.13.2 |

## 4 前端可视化大屏功能需求（Vue3+DataV，高端动态浮动页面）

### 4.1 页面整体设计规范（用于 HR 线上演示，质感拉满）

1. 布局：全屏自适应数字大屏，深蓝色商务科技风，渐变背景
2. 动态浮动效果（核心，区别静态页面）
   - 所有数据卡片、图表模块开启 CSS 上下悬浮浮动动画，周期 3s 缓慢浮动
   - 卡片 hover 放大、发光边框特效
   - 大屏边角装饰线条持续流动动画（DataV 边框）
3. 数字动画：所有统计数字滚动加载，从 0 平滑跳至真实数值
4. 自动轮播：模型对比图表、用户分层饼图每 10s 自动切换展示
5. 实时刷新：页面每 30s 自动请求后端 screen_api，更新全部数据，无需手动刷新
6. 侧边筛选面板：可筛选会员等级、城市层级，筛选后实时重绘所有图表

### 4.2 大屏页面分区模块设计（ScreenDash.vue 主页面）

#### 顶部标题区（全屏通栏）

- 项目大标题：融合 PCA 与因子分析的电商用户复购预测可视化平台
- 实时系统时间、后端服务连接状态、总样本量全局指标卡片（浮动动画）

#### 左上区域：用户全局统计浮动卡片组（4 张浮动卡片）

1. 总用户数量、复购用户、流失用户、整体复购转化率（滚动数字动画）
2. 近 30 天静默用户占比、付费 VIP 复购率对比

#### 左中区域：用户分层分布图表

1. 饼图：不同会员等级复购人数分布

2. 环形图：一线 / 二线 / 三四五线城市用户复购比例

   

   图表模块持续缓慢浮动，hover 显示详细数值弹窗

#### 左下区域：原始消费特征分析

1. 箱线图：用户累计消费金额分布
2. 柱状图：优惠券使用次数与复购关联性

#### 右上区域：降维算法分析看板（项目核心展示，HR 重点观看）

1. PCA 碎石图：主成分方差贡献率折线图

2. 因子载荷散点图：展示各原始特征潜在因子分布

3. 热力图：36 维原始特征相关性热力图

   

   模块浮动 + 自动轮播切换三张图表

#### 右中区域：双模型对比核心看板

1. ROC 曲线对比图：SVM、随机森林 AUC 曲线同框展示
2. 横向柱状图：两种模型准确率、F1、召回率指标对比
3. Top10 特征重要性柱状图（随机森林输出）

#### 右下区域：实时预测演示面板

- 简易输入框，可自定义输入用户消费特征，向后端发送预测请求
- 实时展示该用户复购概率，文字高亮提示高 / 低复购风险

#### 底部通栏：数据日志滚动栏

- 滚动字幕展示后端模型训练耗时、数据清洗记录、接口请求日志，模拟真实线上平台

### 4.3 前端交互功能

1. 一键导出：页面按钮调用后端接口，下载模型对比报告、高清分析图表
2. 手动刷新按钮：强制拉取后端最新数据
3. 全屏切换：大屏一键全屏展示，适合演示给 HR
4. 筛选过滤：侧边多选框筛选用户群体，所有图表实时联动重绘
5. 图表下载：每个图表右上角自带下载按钮，保存 png 图片

## 5 非功能需求

### 5.1 性能需求

1. 后端单次数据清洗接口响应≤800ms（8500 条数据）
2. PCA + 因子分析融合降维计算≤500ms
3. SVM、随机森林同时训练完成≤2s
4. 大屏实时统计接口响应≤200ms，保障 30s 流畅自动刷新
5. 前端页面加载完成≤1.5s，浮动动画无卡顿，适配 1080P/2K 显示器

### 5.2 兼容性需求

1. 后端严格适配 Python 3.13.2，依赖版本无冲突
2. 前端兼容 Chrome、Edge 主流浏览器，支持电脑端大屏展示（不兼容移动端，定位 PC 演示）
3. 系统支持 Windows、Linux 双系统部署

### 5.3 部署需求（HR 线上演示核心）

1. 后端 FastAPI 支持 uvicorn 一键启动，开放 8000 端口
2. 前端 Vue 项目可打包 dist 静态文件，nginx 部署
3. 支持本地单机部署，也可部署至云服务器，外网可直接访问大屏页面
4. 无需复杂数据库，全部数据存储本地 csv 文件，开箱即用，演示零配置

### 5.4 易用性需求

1. 启动文档 README 提供极简启动命令，HR 可一键运行整套系统
2. 前端页面无复杂操作，可视化内容直观易懂，不懂代码也能看懂模型结果
3. 后端控制台打印清晰日志：数据加载、训练进度、接口访问记录

### 5.5 扩展性需求

1. 可扩充更多机器学习模型（逻辑回归、XGBoost）接入对比模块
2. 可新增更多用户特征，降维模块无需大幅修改
3. 前端可新增更多可视化图表，复用现有浮动卡片组件

## 6 接口简要清单（RESTful，FastAPI 自动生成 Swagger 文档 /docs）

1. 数据接口：/api/data/load、/api/data/clean、/api/data/export
2. 降维接口：/api/dim/pca、/api/dim/factor、/api/dim/fusion、/api/dim/plot
3. 建模接口：/api/model/train、/api/model/compare、/api/model/predict、/api/model/report
4. 大屏专用接口：/api/screen/global、/api/screen/user_layer、/api/screen/model_metric
5. 文件静态资源接口：/static/ 存放后端生成图表

## 7 安全需求（演示系统轻量化安全）

1. 后端开启跨域 CORS，允许前端 Vue 访问接口
2. 限制文件导出大小，防止超大文件占用内存
3. 禁止外部上传恶意文件，仅读取本地固定数据集
4. 无用户登录鉴权（纯演示项目，简化流程，HR 直接打开即用）

## 8 交付物需求

1. 数据集生成脚本 + 8500 条 csv 数据集
2. 完整 FastAPI 后端代码，含全部算法模块
3. Vue3 动态浮动可视化大屏前端完整工程
4. 三份文档：数据集说明书、需求规格说明书、项目开发方案
5. README 部署启动教程，HR 演示操作指南

# 第三部分后续预告

确认本需求说明书无误后，我将生成第三份完整文档：《项目开发方案》，包含开发周期、分阶段任务、开发流程、测试方案、部署方案、项目创新点、演示讲解话术（求职 HR 专用）。

你可以回复确认继续生成开发方案。