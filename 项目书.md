# 融合 PCA 与因子分析的电商用户复购预测系统

> **项目定位**：机器学习全流程工程化实战项目
> **一句话介绍**：基于 8500 名电商用户的多维行为数据，通过 PCA + 因子分析融合降维，构建 SVM 与随机森林双模型对比预测 30 天复购行为，并以 FastAPI + Vue3 可视化大屏完成全流程工程落地。

---

## 目录

1. [项目概述](#1-项目概述)
2. [项目背景与业务价值](#2-项目背景与业务价值)
3. [技术栈](#3-技术栈)
4. [系统总体架构](#4-系统总体架构)
5. [数据集与数据解读](#5-数据集与数据解读)
6. [数据预处理](#6-数据预处理)
7. [特征降维：PCA + 因子分析融合（项目核心）](#7-特征降维pca--因子分析融合项目核心)
8. [建模与评估](#8-建模与评估)
9. [实验结果分析](#9-实验结果分析)
10. [可视化大屏](#10-可视化大屏)
11. [项目亮点与难点](#11-项目亮点与难点)
12. [局限与改进方向](#12-局限与改进方向)
13. [面试答辩要点（Q&A）](#13-面试答辩要点qa)
14. [运行方式](#14-运行方式)

---

## 1. 项目概述

电商平台积累海量用户的多维行为数据，但特征之间存在**多重共线性**，直接建模存在计算量大、特征冗余、模型解释性差的问题。

本项目的核心思路是：

1. **降维前置**：先对 36 维原始特征做 PCA 主成分分析与探索性因子分析（EFA），保留数据主要方差的同时提取具有业务含义的"潜在消费因子"，两者**拼接融合**得到更优的模型输入；
2. **双模型对比**：在统一的数据集、统一的评估口径下，分别训练 **SVM** 与 **随机森林**，横向对比其在"30 天复购预测"任务上的表现；
3. **工程化落地**：用 FastAPI 封装全部算法与统计接口，开发 Vue3 动态可视化大屏，实现从"数据生成 → 数据清洗 → 降维 → 建模 → 可视化"的一体化系统。

**核心成果**：

| 模型 | 准确率 | 精确率 | 召回率 | F1 | AUC |
|:---:|:---:|:---:|:---:|:---:|:---:|
| SVM (RBF) | 0.616 | 0.631 | 0.611 | 0.621 | **0.664** |
| 随机森林 | 0.621 | **0.640** | 0.602 | 0.621 | **0.667** |

> 双模型在复购任务上表现接近，随机森林在精确率与 AUC 上略优，体现了树模型对高维非线性特征的拟合优势。

---

## 2. 项目背景与业务价值

### 2.1 业务背景

在电商场景中，"复购率"是衡量用户黏性与平台健康度的核心指标：

- **获客成本高**：吸引新用户的成本通常是维系老用户的 5~7 倍；
- **复购即价值**：有复购行为的用户生命周期价值（LTV）显著高于一次性用户；
- **干预窗口期**：若能提前识别"30 天内可能复购"的用户，可针对性地投放优惠券、召回策略，提升运营 ROI。

因此，构建一个**可解释、可部署**的复购预测模型，对精细化运营有直接价值。

### 2.2 技术挑战

1. **高维特征**：36 维特征覆盖用户画像、消费频次、消费金额、交互行为四大类，维度高、信息冗余；
2. **多重共线性**：如 `total_order_num`（总下单次数）与 `month_order_avg`（月均下单量）、`total_consume` 与 `avg_order_price` 之间存在强相关，直接建模会放大系数方差、降低可解释性；
3. **脏数据**：真实场景下数据存在缺失值、异常值，需要完整的清洗链路；
4. **落地问题**：算法再好，也要能"被业务使用"——需要接口化、可视化。

---

## 3. 技术栈

| 层次 | 技术 |
|:---|:---|
| 语言 | Python 3.13、JavaScript / Vue3 |
| 数据处理 | NumPy、Pandas、SciPy |
| 机器学习 | scikit-learn（PCA、FactorAnalysis、SVC、RandomForestClassifier、预处理、评估） |
| 可视化 | Matplotlib、Seaborn、ECharts 5、DataV |
| 后端服务 | FastAPI、Uvicorn、Pydantic |
| 模型持久化 | Joblib |
| 前端框架 | Vue 3 + Vite、Element Plus、GSAP |
| 数据存储 | CSV / Parquet |
| 工程规范 | RESTful 接口、Swagger 自动文档、前后端分离 |

---

## 4. 系统总体架构

采用**前后端分离**架构：

![image-20260813094304997](C:\Users\PC\AppData\Roaming\Typora\typora-user-images\image-20260813094304997.png)

```
**后端接口分层**（对应四组 RESTful 接口）：
```

| 分组 | 前缀 | 主要接口 |
|:---|:---|:---|
| 数据管理 | `/api/data` | 数据集概览、分页预览、清洗、导出 |
| 降维分析 | `/api/dim` | PCA、因子分析、融合降维、相关性热力图 |
| 模型训练 | `/api/model` | 训练、双模型对比、单用户预测、特征重要性、报告导出 |
| 大屏统计 | `/api/screen` | 全局指标、用户分层、消费统计、模型指标、Top10 特征 |

---

## 5. 数据集与数据解读

### 5.1 数据规模与构成

- **样本**：8500 条用户记录（`dataset/ecommerce_repurchase_8500.csv`）
- **字段**：36 个特征 + 1 个标签列（`repurchase_label`）
- **标签定义**：`1` = 30 天内产生复购，`0` = 无复购
- **正负样本比**：约 51 : 49（均衡，无需重采样）
- **缺失值**：随机注入约 0.8% 缺失（模拟真实脏数据）

### 5.2 特征体系（四大类）

| 分组 | 字段示例 |
|:---|:---|
| ① 用户画像（8） | `user_age`、`user_gender`、`member_level`、`register_days`、`device_type`、`city_tier`、`is_vip` |
| ② 消费频次（10） | `total_order_num`、`month_order_avg`、`week_visit_cnt`、`cart_add_cnt`、`return_order_num`、`refund_times`、`silence_days` |
| ③ 消费金额（10） | `total_consume`、`avg_order_price`、`max_single_order`、`discount_save_money`、`coupon_use_cnt`、`high_price_goods_ratio` |
| ④ 交互行为（8） | `good_comment_cnt`、`live_watch_duration`、`short_video_click`、`consult_service_cnt`、`share_product_cnt`、`search_word_cnt` |

> 特征覆盖了电商用户的全生命周期行为（注册→浏览→加购→下单→复购→售后），设计上满足 PCA / 因子分析"变量间存在相关结构"的前提。

### 5.3 关键数据解读（来自系统真实统计）

| 指标 | 数值 | 业务含义 |
|:---|:---|:---|
| 总用户 / 复购用户 | 8500 / 4371 | 整体复购率 **51.4%** |
| 流失用户 | 4129 | 与复购用户基本对半 |
| 静默用户（≥30 天未下单） | 7704（90.6%） | 绝大多数用户存在高静默，召回空间大 |
| VIP 用户复购率 | 50.3% | 付费会员并未显著提升复购，值得运营反思 |
| 平均消费 / 中位消费 | ¥521.9 / ¥150.0 | 消费呈长尾分布（均值远高于中位数） |
| **复购用户均消** | **¥640.7** | 复购用户比流失用户（¥396.1）**消费高约 62%**，复购价值的直接证据 |
| 优惠券使用率 | 97.1% | 促销工具渗透率极高，是重要的运营抓手 |
| 高价商品占比均值 | 31.4% | 平台客单价结构相对均衡 |

**数据洞察示例**：

1. **复购 = 高价值**：复购用户平均消费高出 62%，"识别复购人群"≈"识别高价值人群"，预测模型直接指向运营 ROI；
2. **静默风险**：90.6% 用户有超过 30 天的静默期，"静默天数"自然成为预测模型最重要的特征之一；
3. **特征相关性**：消费金额、下单次数、加购数量等特征高度相关（多重共线性），正是引入降维的前提。

---

## 6. 数据预处理

预处理链路位于 `backend/core/data_clean.py`，分为四步：

1. **列自动分类**：按"唯一值数量 ≤ 10"将列划分为**分类编码列**（会员等级、城市层级、设备等）与**连续数值列**；
2. **缺失值填充**：连续列用均值/中位数填充，分类列用众数填充，标签列缺失补 0；
3. **异常值处理**：对连续列按 **3σ 原则**剔除异常（裁剪到边界值而非删除整行，保留样本量）；
4. **特征标准化**：仅对连续数值列做 z-score 标准化（`StandardScaler`）；分类编码列保留原始编码，从而保证后续"用户分层"等业务统计口径不失真。

> **设计取舍说明**：分类列不做标准化、保留原始编码，是为了让大屏的"按会员等级/城市层级分组"统计仍具有业务含义；连续列标准化则是 PCA / 因子分析 / SVM 的前提。

**清洗统计**：8500 行全部保留（异常值被裁剪而非删行），共填充缺失值 2366 个、处理异常值 3237 个。

---

## 7. 特征降维：PCA + 因子分析融合（项目核心）

### 7.1 为什么需要"融合降维"？

| 方法 | 优势 | 不足 |
|:---|:---|:---|
| PCA | 保留数据最大方差，信息无损地压缩维度 | 主成分是特征的线性组合，业务含义模糊 |
| 因子分析 | 提取"潜在公因子"，业务可解释性强 | 以解释变量结构为主，方差保留不如 PCA |
| **PCA + 因子融合** | **兼顾方差保留（信息量）与业务可解释性** | 维度略增，但通常带来更好/更稳的模型效果 |

### 7.2 实现流程（`backend/core/dim_reduce.py`）

```
原始特征 X (8500 × 36)
   │
   ├── PCA 主成分分析 ──► 主成分得分 P (8500 × 33)    [累计方差 96.2%]
   │      （自动选择成分数：累计方差 ≥ 95%）
   │
   ├── 因子分析 ──► 因子得分 F (8500 × 10)          [提取 10 个公因子]
   │      （载荷矩阵 / 公因子方差 communality）
   │
   └── 横向拼接：融合特征 [P | F] (8500 × 43)
          │
          └── 融合后二次标准化 ──► 送入模型训练
```

- **PCA**：先拟合全量 PCA 得到方差贡献率，再自动选择"累计方差 ≥ 95%"的最少主成分数。实测在本数据集上选择 **33 个主成分，累计解释方差 96.2%**，即用 33 维保留了 36 维 96% 的信息；
- **因子分析**：提取 **10 个公因子**，输出因子载荷矩阵（特征×因子）与公因子方差，载荷可用于解释"潜在消费因子"（如金额因子、活跃因子、售后因子）；
- **融合**：将 PCA 主成分得分与因子得分横向拼接，形成 43 维融合特征，再做一次标准化后作为 SVM / 随机森林的输入。

### 7.3 输出

- PCA 碎石图（方差贡献率 + 累计贡献率）
- 因子载荷散点图（前两个公因子）
- 特征相关性热力图
- 融合特征维度与降维评价指标

---

## 8. 建模与评估

### 8.1 实验设置（`backend/core/model_train.py`）

| 项目 | 配置 |
|:---|:---|
| 数据划分 | 训练集 70% / 测试集 30%，**分层采样**，随机种子固定（42）保证可复现 |
| SVM | RBF 核，`C=1.0`，`gamma='scale'`，开启概率输出 |
| 随机森林 | 100 棵树，最大深度 10 |
| 评估指标 | 准确率、精确率、召回率、F1、**AUC**、混淆矩阵、ROC 曲线 |

### 8.2 评估指标说明

复购预测是**类别不平衡敏感**任务，单一准确率不足以评价，因此采用综合指标：

- **精确率（Precision）**：预测"会复购"的用户中真实复购的比例 —— 精确率低 → 优惠券发错人、成本浪费；
- **召回率（Recall）**：真实复购用户中被正确找出的比例 —— 召回率低 → 漏掉高价值用户；
- **AUC**：综合反映排序能力，对阈值不敏感，是此类任务的**核心指标**。

### 8.3 单用户实时预测

- 输入：36 个原始特征（逗号分隔）
- 后端按训练口径自动完成"填充 → 标准化 → 预测"
- 输出：复购概率（0~1）、预测标签、风险等级（高/中/低）、耗时

---

## 9. 实验结果分析

### 9.1 双模型对比（融合特征口径）

| 指标 | SVM | 随机森林 | 结论 |
|:---|:---:|:---:|:---|
| 准确率 | 0.616 | 0.621 | 接近 |
| 精确率 | 0.631 | **0.640** | RF 略优 |
| 召回率 | 0.611 | 0.602 | SVM 略优 |
| F1 | 0.621 | 0.621 | 持平 |
| **AUC** | 0.664 | **0.667** | RF 略优 |

### 9.2 特征重要性（随机森林 Top10）

| 排名 | 特征 | 重要性 |
|:---:|:---|:---:|
| 1 | `cart_add_cnt` 累计加购件数 | 0.108 |
| 2 | `silence_days` 静默天数 | 0.102 |
| 3 | `total_order_num` 总下单次数 | 0.080 |
| 4 | `month_order_avg` 月均下单量 | 0.053 |
| 5 | `total_consume` 累计消费 | 0.049 |
| 6 | `register_days` 注册天数 | 0.036 |
| 7 | `discount_save_money` 优惠减免 | 0.034 |
| 8 | `promo_order_ratio` 活动订单占比 | 0.033 |
| 9 | `low_price_goods_ratio` 低价占比 | 0.033 |
| 10 | `live_watch_duration` 直播观看时长 | 0.033 |

**结论解读**：

1. **加购、下单、消费、静默**四大行为占据重要性榜首，与业务直觉完全一致——"用户的行为强度直接决定复购概率"；
2. **静默天数排第 2**：越久不活跃，复购概率越低，与"90.6% 用户存在静默"的数据现状相互印证，提示运营应重点关注**静默召回**；
3. 模型中 **AUC 稳定在 0.66~0.67**，说明降维后的特征对复购行为有真实的区分能力；由于数据为仿真生成、业务逻辑本身信噪比有限，该水平属于预期内的合理区间（重点在演示**完整方法论与工程链路**）。

---

## 10. 可视化大屏

前端大屏（Vue3 + ECharts）实现 **8 大可视化区域**，全自动轮播 + 30s 自动刷新：

| 区域 | 展示内容 |
|:---|:---|
| 顶部 | 项目标题、系统时间、后端连接状态、样本总量 |
| 左上 | 全局 KPI：总用户、复购用户、复购转化率、流失用户（数字滚动动画） |
| 左中 | 会员等级复购分布、城市层级复购分布（饼图） |
| 左下 | 消费金额分析（复购 vs 流失用户均消对比条形图） |
| 中间上 | 降维算法看板：PCA 碎石图 / 因子载荷 / 相关性热力图（Tab 切换） |
| 中间下 | 双模型对比：ROC 曲线、指标对比柱状图 |
| 右上 | 随机森林特征重要性 Top10（横向条形图） |
| 右下 | 实时复购预测 Demo |
| 底部 | 滚动运行日志 |

**视觉设计**：深蓝科技风主题，卡片渐变流光描边 + 顶部流光扫光动画 + 背景极光动效 + 一次性入场动画（无永久浮动，保证阅读稳定）。

---

## 11. 项目亮点与难点

### 亮点

1. **双降维融合策略**：区别于单一 PCA，将 PCA（方差保留）与因子分析（业务可解释）拼接融合，兼顾信息量与解释性；
2. **双模型横向对比**：统一数据、统一降维特征、统一评估口径，科学对比 SVM 与随机森林；
3. **全流程工程化**：不只是算法脚本，而是"数据生成 → 清洗 → 降维 → 建模 → 预测 → 报告导出 → 可视化大屏"的一体化系统；
4. **前后端分离 + 接口化**：FastAPI 封装全部能力，Swagger 自动文档，Vue3 大屏实时渲染，可直接部署演示；
5. **代码质量**：配置中心化（`config.py`）、模块分层（core / api / schemas / utils）、随机种子固定可复现、单元测试入口齐全。

### 难点与解决

| 难点 | 解决方案 |
|:---|:---|
| 36 维特征多重共线性 | PCA + 因子分析融合降维，先降噪再建模 |
| 机器学习项目展示单薄 | 开发动态可视化大屏，全流程数据可视化 |
| 算法无法在线交互演示 | FastAPI 接口化，浏览器即可操作预测 |
| Python 3.13 新库兼容 | requirements 锁定兼容版本，规避 wheel 缺失 |
| 分类列被误标准化导致业务统计失真 | 唯一值数判分类列 + 仅标准化连续列（本项目实际踩坑后修复） |

---

## 12. 局限与改进方向

1. **数据为仿真生成**：业务逻辑可控，但非真实平台数据，模型效果上限有限；后续可接入真实埋点数据验证泛化性；
2. **特征工程可深化**：当前以原始/降维特征建模，可增加 RFM、最近一次消费距今天数、商品品类偏好等衍生特征；
3. **算法可升级**：可对比 XGBoost / LightGBM / 神经网络，或引入 SMOTE 处理类别不平衡、交叉验证调参（GridSearch）；
4. **时序信息**：当前是截面数据，可引入用户行为时间序列，做复购概率的动态更新；
5. **上线保障**：补充模型监控（PSI 漂移）、AB 实验设计、与营销触达系统的接口对接。

---

## 13. 面试答辩要点（Q&A）

**Q1：为什么不用单一 PCA 或单一因子分析？**
> PCA 保留方差信息但不解释业务，因子分析可解释但方差保留有限。二者融合 = 主成分的"信息量" + 公因子的"业务含义"，在信息保留与可解释性之间取得平衡。这是本项目区别于常规降维方案的创新点。

**Q2：SVM 和随机森林哪个更适合这个场景？**
> 实测两者 AUC 接近（0.664 vs 0.667）。随机森林对高维非线性特征更鲁棒、无需标准化、自带特征重要性，工程上更易落地；SVM 在样本量中等、边界清晰时精度有优势。项目价值在于建立了**可复现的对比实验框架**，业务上线时可据此选择。

**Q3：特征重要性揭示了什么业务洞察？**
> 加购、下单、消费、静默四大行为占据 Top4。特别是"静默天数"排第 2，且数据中 90.6% 用户存在 30 天以上静默——说明运营的核心抓手是**静默用户召回**，而非单纯拉新。

**Q4：如何处理类别不平衡？**
> 本数据集正负样本 51:49 基本均衡，无需重采样。若未来数据失衡，我会优先考虑：调整分类阈值（而非固定 0.5）、使用 AUC/PR 作为评估主指标、必要时采用 SMOTE 或代价敏感学习。

**Q5：系统怎么做到可复现、可部署？**
> 随机种子全局固定（config 集中管理）、数据划分分层采样、依赖版本锁定；后端 FastAPI 接口化 + Swagger 文档，前端可 `npm run build` 产出静态资源，支持本地 / 云服务器部署。

**Q6：你在这个项目里实际解决了什么问题？（可讲踩坑）**
> 例如：① NaN 注入后 int 列被 pandas 转成 float64，导致分类列被误判为连续列并被标准化，用户分层统计全部失真——通过"唯一值数量"而非 dtype 判断分类列解决；② 前端图表容器在 flex 布局下高度解析为 0 导致图表不渲染——改为全 flex 自适应布局解决；③ 单用户预测维度不匹配（融合特征 43 维 vs 用户输入 36 维）——统一采用原始特征 + 训练口径缩放解决。

---

## 14. 运行方式

```bash
# 1. 安装依赖
pip install -r backend/requirements.txt
cd frontend && npm install && cd ..

# 2. 一键启动（Windows）
start.bat

# 3. 或分开启动
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000   # 后端
cd frontend && npm run dev                                        # 前端

# 4. 访问
#    可视化大屏：http://127.0.0.1:3000
#    API 文档：  http://127.0.0.1:8000/docs
```

---

## 附：项目目录结构

```
ecommerce_repurchase_system/
├─ backend/
│  ├─ main.py                 # FastAPI 服务入口
│  ├─ config.py               # 全局配置（路径、超参、随机种子）
│  ├─ core/
│  │  ├─ data_clean.py        # 数据清洗（缺失/异常/标准化）
│  │  ├─ dim_reduce.py        # PCA / 因子分析 / 融合降维
│  │  ├─ model_train.py       # SVM / RF 训练、评估、预测
│  │  └─ visual_draw.py       # matplotlib/seaborn 绘图
│  ├─ api/                    # data / dim / train / screen 四组接口
│  ├─ schemas/                # Pydantic 数据模型
│  └─ utils/                  # 文件、数学工具
├─ frontend/
│  ├─ src/views/ScreenDash.vue # 可视化大屏主页面
│  ├─ src/api/index.js         # axios 接口封装
│  └─ src/components/          # FloatCard / ChartBox / NumScroll 等
├─ data_generate/generate_dataset.py  # 仿真数据集生成脚本
├─ dataset/                          # 8500 条数据集
├─ output/                           # 模型、报告、图表产出
├─ docs/                             # 需求/数据/开发三份说明书
└─ 项目书.md                         # 本文档
```
