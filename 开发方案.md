# 第三部分：融合 PCA 与因子分析的电商用户复购预测系统 项目开发方案

## 文档基础信息

1. 对应项目：融合 PCA 与因子分析的电商用户复购预测（SVM、随机森林对比建模）
2. Python 运行版本：3.13.2
3. 架构：前后端分离（FastAPI 后端 + Vue3 DataV 动态可视化大屏）
4. 适用场景：毕业设计、个人作品集、线上部署 HR 演示
5. 配套文档：数据集说明书、需求规格说明书、本开发方案

# 一、项目总体概述

## 1.1 项目背景

电商平台积累海量用户多维度行为数据，高维特征存在严重多重共线性，直接训练分类模型存在计算量大、特征冗余、解释性差问题。本项目融合 PCA 主成分分析与因子分析完成双层降维，提取用户消费潜在因子，基于降维融合特征分别搭建 SVM、随机森林二分类模型，对比两种算法在用户 30 天复购预测任务下的效果；通过 FastAPI 封装全部算法与数据接口，搭配动态浮动高端可视化大屏，直观展示数据分布、降维结果、模型评估指标，完整实现从数据集生成、数据清洗、特征降维、模型训练、可视化展示一体化工程系统。

## 1.2 项目核心创新点（HR 面试重点亮点）

1. **双降维融合策略**：区别单一 PCA / 因子分析，将 PCA 主成分得分与因子分析公因子特征拼接融合，兼顾数据方差保留与业务可解释性，优化模型输入特征；
2. **双模型横向对比实验**：统一数据集、统一降维特征、统一评估指标，定量对比 SVM 与随机森林在电商复购预测场景优劣；
3. **工程化完整前后端落地**：不只是单纯机器学习代码，搭建 FastAPI 工业级接口服务，配套动态浮动 3D 动画可视化大屏，可线上部署外网访问；
4. **全流程自动化**：内置数据集生成、缺失值清洗、标准化、降维、训练、绘图、报告导出一体化工具链；
5. **高适配演示设计**：前端全部模块浮动动画、数字滚动、自动轮播，无需操作即可完整展示项目核心成果，适配 HR 线上查看。

## 1.3 开发目标

1. 生成 8500 条含 36 维特征 + 复购标签的仿真电商数据集，自带缺失值与业务逻辑关联；
2. 完成数据预处理模块：缺失填充、异常值过滤、特征标准化；
3. 实现 PCA、因子分析独立降维及融合特征构建；
4. 完成 SVM、随机森林模型训练、预测、多维度指标评估；
5. FastAPI 封装所有算法、绘图、统计接口，自动生成 Swagger 接口文档；
6. 开发 Vue3 动态可视化大屏，全页面浮动动画，实时拉取后端数据渲染图表；
7. 支持 Windows/Linux 本地部署、云服务器线上部署，提供一键启动脚本，开箱即用；
8. 产出全套技术文档，可直接用于技术社区发布、求职作品集展示。

## 1.4 技术栈总览

### 后端 Python（3.13.2）

数据处理：numpy、pandas

可视化绘图：matplotlib、seaborn

机器学习 / 降维：scikit-learn（PCA、FactorAnalysis、SVC、RandomForest、预处理、评估指标）

数值计算：scipy

Web 服务：fastapi、uvicorn

数据持久化：joblib（模型保存）、pyarrow（parquet）

数据校验：pydantic

文件工具：openpyxl

### 前端

基础框架：Vue3 + Vite

大屏组件：DataV

图表库：ECharts 5

动画：GSAP、CSS 浮动动画

UI：Element Plus

网络请求：Axios

样式：SCSS

# 二、项目开发整体流程规划

整体分为 5 大开发阶段，总周期 22 天，分阶段交付成果，可直接写进报告

## 阶段 1：数据层开发（3 天）

任务清单：

1. 梳理 36 维用户特征分类，定义复购标签业务生成逻辑；

2. 编写`generate_dataset.py`数据集生成脚本，配置 8500 样本、0.8% 随机缺失值；

3. 测试数据集输出，校验特征相关性、正负样本均衡性、缺失值分布；

4. 编写《数据集详细说明书》，完善字段释义、数据分布规则；

   

   交付产物：数据集 csv 文件、数据集生成代码、数据集说明书。

## 阶段 2：后端核心算法模块开发（7 天）

任务清单：

1. 搭建后端目录结构，创建 config 全局配置、工具类 utils；

2. 开发数据清洗模块：缺失值填充、异常值裁剪、标准化；

3. 开发降维核心模块：独立 PCA、独立因子分析、PCA + 因子融合特征逻辑；

4. 开发建模模块：数据集划分、SVM 训练、随机森林训练、多评估指标计算；

5. 开发绘图工具模块：matplotlib/seaborn 绘制热力图、碎石图、ROC 曲线等，图片转 base64；

6. 编写 requirements.txt，适配 Python3.13.2 所有依赖；

   

   交付产物：后端 core 全部算法代码、工具类、依赖清单。

## 阶段 3：FastAPI 接口层开发（4 天）

任务清单：

1. 搭建 pydantic 数据校验模型 schemas；

2. 分层开发 4 组接口：数据接口、降维接口、模型训练接口、大屏专用统计接口；

3. 配置跨域 CORS、静态文件路由、全局异常捕获；

4. 编写 main.py 服务入口，配置 uvicorn 启动参数；

5. 接口功能全量测试，校验返回数据、图片、文件导出功能；

   

   交付产物：完整 api 路由代码、可直接运行的 FastAPI 服务。

## 阶段 4：前端可视化大屏开发（6 天）

任务清单：

1. 初始化 Vue3+Vite 项目，引入 DataV、ECharts、GSAP；

2. 封装通用组件：浮动卡片、滚动数字、图表容器、侧边筛选面板；

3. 划分大屏六大区域，完成页面布局、深蓝色科技渐变主题样式；

4. 编写全局浮动动画、自动轮播、30s 自动刷新逻辑；

5. 封装 axios 请求所有后端接口，完成图表数据联动渲染；

6. 开发导出报表、单用户复购预测、全屏演示功能；

   

   交付产物：完整前端工程，可打包 dist 静态资源。

## 阶段 5：系统联调、测试、文档、部署优化（2 天）

任务清单：

1. 前后端联调，修复接口传参、图表渲染、数据刷新 bug；

2. 全功能测试：数据生成、清洗、降维、建模、大屏可视化、文件导出；

3. 编写全套文档：需求规格说明书、本开发方案、README 部署教程；

4. 本地 Windows、Linux 双系统部署测试，优化启动命令；

5. 整理项目演示话术、核心成果截图，适配 HR 线上观看；

   

   交付产物：完整项目源码、全套文档、部署教程、演示素材。

# 三、分模块详细开发方案

## 3.1 数据集模块开发方案

### 开发目标

仿真真实电商高维用户数据，特征具备业务逻辑关联，存在多重共线性，适配 PCA 与因子分析降维实验。

### 开发步骤

1. 划分四大类特征：用户画像、消费频次、消费金额、交互行为，合计 36 列；
2. 使用对数正态分布生成消费类长尾数据，贴合电商真实用户消费分布；
3. 构建复购综合得分公式，正向特征提升复购概率，负向特征降低概率，生成 0/1 标签；
4. 随机注入 0.8% 缺失值，模拟平台脏数据，用于数据清洗实验；
5. 脚本内置样本统计输出：行列数、缺失值、标签分布、描述性统计；

### 验收标准

- 样本 8500 条，36 特征 + 1 标签，总 37 列；
- 复购正负样本比例接近 4.5:5.5，样本均衡；
- 特征间存在多重共线性，适合降维；
- 可一键运行生成 csv，无报错，兼容 Python3.13.2。

## 3.2 后端算法核心模块开发方案

### 3.2.1 数据清洗模块 data_clean.py

技术：pandas、numpy、StandardScaler

功能实现：

1. 缺失值区分特征类型填充：数值特征均值填充，分类特征众数填充；
2. 3σ 准则剔除消费金额、下单次数极端异常值；
3. 全部数值特征标准化，消除量纲影响（PCA / 因子分析必须标准化）；
4. 返回清洗后数据集、清洗统计报告（删除异常样本数量、填充缺失数量）。

### 3.2.2 降维模块 dim_reduce.py（项目核心）

技术：sklearn PCA、FactorAnalysis

三层逻辑：

1. 独立 PCA 降维：计算方差贡献率、输出碎石图坐标、主成分矩阵；

2. 独立因子分析：计算公因子方差、因子载荷矩阵、因子得分；

3. 融合降维逻辑：将 PCA 主成分得分与因子分析得分横向拼接，生成融合新特征，作为 SVM、随机森林输入；

   

   输出：降维后特征、降维评价指标（累计方差、载荷系数）。

### 3.2.3 建模模块 model_train.py

技术：SVC、RandomForestClassifier、train_test_split、metrics

统一实验标准：训练集 70%，测试集 30%，随机种子固定保证复现；

1. 随机森林：可配置树数量、最大深度，输出特征重要性数组；
2. SVM：支持高斯核 rbf，自动超参基础配置；
3. 统一评估指标：准确率、精确率、召回率、F1 分数、AUC；
4. 模型持久化：使用 joblib 保存训练完成的 SVM、随机森林模型至 output/model；
5. 模型对比：将两类模型指标整合为统一对比表格，支持导出 csv 报告。

### 3.2.4 绘图模块 visual_draw.py

技术：matplotlib、seaborn

统一流程：绘制画布→绘图→关闭坐标轴→转 base64 字符串→返回接口；

绘制图表清单：

1. 特征相关性热力图
2. PCA 方差贡献率碎石图
3. 因子载荷散点图
4. SVM & 随机森林 ROC 对比曲线
5. 随机森林 Top10 特征重要性柱状图
6. 用户消费金额分布箱线图

## 3.3 FastAPI 接口开发方案

### 接口分层设计

1. 数据接口 /api/data
   - 加载数据集、预览分页数据、数据清洗、导出清洗后 csv
2. 降维接口 /api/dim
   - 单独 PCA、单独因子分析、融合降维、获取降维图表
3. 模型接口 /api/model
   - 模型训练、双模型指标对比、单用户复购预测、导出评估报告
4. 大屏专用接口 /api/screen（轻量化，低延迟）
   - 全局用户统计、用户分层分布、模型实时指标、特征重要性 Top10

### 配套功能

1. 全局跨域，支持前端 Vue 访问；
2. 自动生成 Swagger 在线接口文档：[http://127.0.0.1:8000/docs](https://link.wtturl.cn/?target=http%3A%2F%2F127.0.0.1%3A8000%2Fdocs&scene=im&aid=497858&lang=zh)
3. 全局异常捕获，统一返回错误信息；
4. /static 静态路由，缓存后端生成图表。

## 3.4 前端可视化大屏开发方案（高端浮动动态页面）

### 页面核心设计规范

1. 主题：深蓝科技商务风，渐变背景、发光边框、DataV 流动装饰边框；
2. 动态特效（区别静态页面，HR 重点加分项）：
   - 全部图表卡片 3s 周期上下缓慢浮动 CSS 动画；
   - 卡片 hover 放大 + 白光发光特效；
   - 统计数字滚动动画，从 0 递增至真实数值；
   - 图表每 10s 自动轮播切换；
   - 页面每 30s 自动请求后端接口刷新全部数据；
3. 布局分区：顶部标题区、左上全局指标、左中用户分层、左下消费分布、右上降维看板、右中模型对比、右下预测演示、底部滚动日志。

### 核心组件封装

1. FloatCard.vue：通用浮动数据卡片，全局复用；
2. NumScroll.vue：数字滚动动画组件；
3. ChartBox.vue：带浮动动画的图表容器；
4. SidePanel.vue：筛选面板，支持会员、城市层级筛选，图表联动刷新。

### 交互功能

1. 一键全屏演示、手动刷新数据；
2. 图表单独下载 PNG 图片；
3. 一键导出模型对比报表；
4. 自定义用户特征输入，实时预测复购概率。

# 四、系统测试方案

## 4.1 单元测试

1. 数据集生成脚本测试：校验行列、缺失值、标签分布；
2. 算法模块单元测试：清洗、PCA、因子分析、模型训练单独运行无报错；
3. 工具函数测试：文件读写、图片 base64 转换。

## 4.2 接口功能测试

使用 FastAPI 自带 /docs 在线测试所有接口，校验：

1. 数据接口正常返回表格、支持文件下载；
2. 降维接口返回指标与图表图片；
3. 模型接口完成训练，返回完整评估指标；
4. 大屏接口快速返回轻量化统计数据，响应速度 < 200ms。

## 4.3 前后端联调测试

1. 前端所有图表正常渲染后端 base64 图片；
2. 自动刷新、筛选、轮播动画无卡顿；
3. 预测面板提交特征可正常返回复购概率；
4. 导出文件功能正常。

## 4.4 兼容性测试

1. 后端：Windows11、Linux Ubuntu，Python3.13.2 运行无依赖冲突；
2. 前端：Chrome、Edge 浏览器，1080P/2K 显示器自适应大屏布局。

# 五、部署实施方案（HR 线上演示专用）

## 5.1 本地单机部署（推荐 HR 本地演示）

### 后端部署步骤

1. 安装 Python3.13.2；
2. 执行依赖安装：`pip install -r backend/requirements.txt`；
3. 运行数据集脚本生成数据：`python data_generate/generate_dataset.py`；
4. 启动 FastAPI 服务：

bash







```
cd backend
uvicorn main:app --host 0.0.0.0 --port 8000
```

1. 访问接口文档：[http://localhost:8000/docs](https://link.wtturl.cn/?target=http%3A%2F%2Flocalhost%3A8000%2Fdocs&scene=im&aid=497858&lang=zh)

### 前端部署步骤

1. 安装 Node.js 18+；
2. 进入 frontend 目录安装依赖：`npm install`；
3. 本地开发启动：`npm run dev`；
4. 打包静态资源（线上部署）：`npm run build`，产出 dist 文件夹。

## 5.2 云服务器线上部署（外网访问，HR 远程查看）

1. 后端：服务器安装 Python3.13.2，后台常驻运行 uvicorn，开放 8000 端口；
2. 前端 dist 文件夹使用 Nginx 托管，80 端口访问可视化大屏；
3. 配置服务器安全组放行端口，外网可直接访问大屏页面，无需本地环境。

# 六、项目成果与交付清单

## 6.1 源码文件

1. 数据集生成脚本 generate_dataset.py + 8500 条 csv 数据集
2. 完整 FastAPI 后端工程（算法、接口、工具、配置）
3. Vue3 动态可视化大屏前端完整工程

## 6.2 全套技术文档

1. 《数据集详细说明书》
2. 《需求规格说明书》
3. 《项目开发方案》（本文档）
4. README.md 部署与演示教程

## 6.3 运行产出文件

1. output/model：训练完成 SVM、随机森林模型文件
2. output/report：模型对比评估报表 csv
3. output/image：后端生成全部分析图表

## 6.4 演示素材（求职加分）

1. 大屏动态效果图截图、接口文档截图、模型指标对比截图
2. 项目讲解话术（适配 HR 面试、线上演示）

# 七、项目难点与解决方案

1. **难点 1：36 维高维特征多重共线性，降维效果差**

   

   方案：采用 PCA + 因子分析融合降维，PCA 保留数据方差，因子分析提取业务可解释消费因子，融合后特征同时兼顾信息量与解释性，优于单一降维。

2. **难点 2：机器学习项目只有代码无可视化，展示效果单薄**

   

   方案：开发动态浮动高端大屏，全流程数据可视化，自动刷新、动画效果丰富，直观展示项目完整成果，HR 直观看懂项目价值。

3. **难点 3：算法代码无法线上交互演示**

   

   方案：FastAPI 封装全部算法为 Web 接口，前后端分离架构支持外网部署，任何人打开浏览器即可在线操作、查看模型结果。

4. **难点 4：Python3.13.2 新版本部分库兼容问题**

   

   方案：requirements.txt 锁定适配新版本的 numpy、scikit-learn、fastapi 版本，规避版本冲突。

